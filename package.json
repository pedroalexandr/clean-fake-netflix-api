{
  "name": "clean-fake-netflix-api",
  "version": "1.0.0",
  "description": "A fake Netflix API applying concepts such as Clean Architecture, TDD, SOLID.",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --colors --noStackTrace --passWithNoTests",
    "test:unit": "npm test -- --watch",
    "test:integration": "jest --watch --silent --colors --noStackTrace",
    "test:staged": "npm test -- --findRelatedTests",
    "test:ci": "npm test -- --coverage"
  },
  "standard": {
    "env": ["jest"]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^5.0.6",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.3",
    "standard": "^16.0.3"
  }
}
